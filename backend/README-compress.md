# PDF Compress API Backend

FastAPI backend for PDF compression service.

## Deployment

Deploy to Hugging Face Spaces with Docker SDK. Use `Dockerfile.compress` and `requirements-compress.txt`.

## Local Development

```bash
pip install -r requirements-compress.txt
python pdfcompress.py
```

API: http://localhost:7860
