version: '3.8'

services:
  pdf-converter:
    build: .
    ports:
      - "10000:10000"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:10000/health"]
      interval: 30s
      timeout: 10s
      retries: 3